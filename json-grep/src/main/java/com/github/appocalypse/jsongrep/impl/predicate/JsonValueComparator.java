package com.github.appocalypse.jsongrep.impl.predicate;

import java.util.Comparator;

import javax.json.JsonValue;

public class JsonValueComparator implements Comparator<JsonValue> {
	@Override
	public int compare(JsonValue left, JsonValue right) {
		// TODO:
		return 0;
	}
}